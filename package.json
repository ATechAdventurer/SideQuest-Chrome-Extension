{
  "name": "SideQuest-Bsaber-Extension",
  "version": "0.0.1",
  "description": "An extension that allows you to install bsaber maps into sidequest",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  }
}
